/**
 * This file includes polyfills needed by Angular and is loaded before the app.
 * You can add your own extra polyfills to this file if the ones listed below do not apply to you.
 * This file is divided into 2 sections:
 *   1. Browser polyfills. These are applied before loading ZoneJS and are sorted by browsers.
 *   2. Application imports. Files imported after ZoneJS that should be loaded before your main
 *      file.
 *
 * The current setup is for so-called "evergreen" browsers; the last versions of browsers that
 * automatically update themselves. This includes recent versions of Chrome, Edge (based on Chromium),
 * Firefox and Safari on desktop, iOS Safari and Chrome on mobile.
 *
 * Learn more in https://angular.io/guide/browser-support
 */

/***************************************************************************************************
 * BROWSER POLYFILLS
 */

/**
 * By default, zone.js will patch all possible macroTask and (macro) task-like APIs.
 * This includes the following APIs:
 * - setTimeout
 * - setInterval
 * - setImmediate
 * - requestAnimationFrame
 * - new Promise() constructor
 * - Promise.resolve(value)/Promise.reject(reason)
 * - Promise.race(values)
 * - every static method of Promise
 * - every method of Promise.prototype
 * - Promise.allSettled() constructor
 * - Promise.any() constructor
 * - Promise.prototype.finally() method
 * - resolving Observable returned by Angular's HttpClient
 * - other APIs added by additive libraries
 * - Interceptors from HttpClientModule
 */


/***************************************************************************************************
 * Zone JS is required by default for Angular itself.
 */
// Included with Angular CLI.


/***************************************************************************************************
 * APPLICATION IMPORTS
 */
